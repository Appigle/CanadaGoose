# Database Configuration for CanadaGoose
# Copy this file to .env and fill in your actual values
# These values are stored in AWS Secrets Manager: canadagoose-prod-app-secrets-09712ff2

# RDS Database Connection
DB_HOST=canadagoose-prod-db.cozaqoges4eb.us-east-1.rds.amazonaws.com
DB_PORT=3306
DB_USER=webapp_user
DB_PASSWORD=5DoFtlBfaWaonf0t
DB_NAME=webapp_db

# JWT Secret (for authentication)
JWT_SECRET=bqLYMgJfzyYgErabNZ9Ljm87Ka2DTEd9

# Server Configuration
NODE_ENV=production
PORT=3000

# URL Configuration
# Note: Server runs on localhost:3000 internally
# External users access via s25cicd.xiaopotato.top (handled by Nginx)
FRONTEND_URL=http://s25cicd.xiaopotato.top
CORS_ORIGIN=http://s25cicd.xiaopotato.top

# Optional: Logging
LOG_LEVEL=info

# Optional: Database Connection Pool
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0
DB_WAIT_FOR_CONNECTIONS=true

# Optional: Security
BCRYPT_ROUNDS=12
SESSION_SECRET=your-session-secret-here

# Optional: Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: CORS
CORS_CREDENTIALS=true

# Development CORS (set NODE_ENV=development for local development)
# NODE_ENV=development 